let miArrayNotas = [5.4, 5, 3, 2, 2.4, 6, 6, 9];
let miArraydeAprobados=[];

for (let i = 0; i < miArrayNotas.length; i++) {
    let nota = miArrayNotas[i];
    
    if (nota >= 5) {
        miArraydeAprobados.push[nota];
    }  
}